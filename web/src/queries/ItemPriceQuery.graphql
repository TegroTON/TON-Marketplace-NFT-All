query itemPrice($address: String!) {
    item(address: $address) {
        address
        sale {
            address
            fullPrice
            networkFee
        }
        owner {
            address
        }
    }
}
