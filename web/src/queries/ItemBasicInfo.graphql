query itemBasicInfo($address: String!) {
    item(address: $address) {
        address
        index
        image
        name
        sale {
            address
            fullPrice
        }
    }
}
