package money.tegro.market.tool

import com.github.ajalt.clikt.core.CliktCommand

class CollectionCommand : CliktCommand(name = "collection", help = "Operations on NFT collections") {
    override fun run() {
        // Nothing
    }
}
