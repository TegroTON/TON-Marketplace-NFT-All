package money.tegro.market.tool

import com.github.ajalt.clikt.core.CliktCommand

class ItemCommand : CliktCommand(name = "item", help = "Operations on single NFT items") {
    override fun run() {
        // Nothing
    }
}
